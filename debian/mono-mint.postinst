#!/bin/sh -e

# if we're configuring, /usr/share/doc/mono-interpreter is a directory,
# and isn't a symlink, replace it with a symlink to mono-common
if [ "$1" = "configure" -a -d /usr/share/doc/mono-interpreter -a ! -h /usr/share/doc/mono-interpreter ]; then
	rmdir /usr/share/doc/mono-interpreter
	ln -s mono-common /usr/share/doc/mono-interpreter
fi

update-alternatives --install /usr/bin/cli cli /usr/bin/mint 10 --slave /usr/share/man/man1/cli.1.gz cli.1.gz /usr/share/man/man1/mint.1.gz

#DEBHELPER#
